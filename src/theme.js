import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    primary: {
      light: "#f1aaff",
      main: "#bc79ff",
      dark: "#894acb",
      contrastText: "#fff",
    },
    secondary: {
      light: "#b6ffff",
      main: "#81d4fa",
      dark: "#4ba3c7",
      contrastText: "#fff",
    },
  },
});
