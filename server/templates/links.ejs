<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        :root {
            /* color variables */
            --btn-text-color: #2f2f2f;
            --btn-bg-color: #ffd0d9;
            --btn-hover-bg-color: #ffb0b9;
            /* text font variables */
            --btn-font-size: 1rem;
            --btn-font-weight: 500;
            --btn-font-family: 'Times New Roman', Times, serif; /* arbitrary */
        }
        .links-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }
        .link-buttons {
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 8px 16px;
            min-height: 50px;
            text-decoration: none;
            background-color: var(--btn-bg-color);
            color: var(--btn-text-color);
            font-family: var(--btn-font-family);
            font-size: var(--btn-font-size);
            font-weight: 500;
            width: 250px;
            border-radius: 5px;
        }
        .link-buttons:hover {
            cursor: pointer;
            background-color: var(--btn-hover-bg-color);
        }
    </style>
</head>

<body>
    <% 
        function cleanUrl(url) {
            return url.replace('https://', '').replace('http://', '').replace("www.", "");
        }
    %>
    <div class="links-container">
        <h1><%= title %></h1>
        <% urls.forEach(function(url) { %>
            <a class="link-buttons" href="<%= url %>" target="_blank" rel="noopener noreferrer">
                <%= cleanUrl(url) %>
            </a>
            <% }); %>
    </div>
</body>

</html>